import { VirtualOfficeWrapper } from '@/components/virtual-office/VirtualOfficeWrapper';

export const metadata = {
  title: 'Virtual Office - Empleaido Factory',
  description: 'Navigate through your company departments in an immersive virtual environment',
};

export default function VirtualOfficePage() {
  return <VirtualOfficeWrapper />;
}
