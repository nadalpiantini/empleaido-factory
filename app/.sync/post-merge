#!/bin/bash
# Git post-merge hook
# Notifies when merge is complete and shows summary

# Colors
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
NC='\033[0m'

echo ""
echo -e "${GREEN}✅ Merge complete!${NC}"
echo -e "${BLUE}📊 Summary:${NC}"
git log -1 --oneline
echo ""
echo -e "${YELLOW}💡 Don't forget to sync with the other server:${NC}"
echo -e "   Run: ${GREEN}.sync/sync.sh${NC}"
echo ""
